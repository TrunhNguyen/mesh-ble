
## 📂 Cấu trúc thư mục dự án
Do thư mục SDK gốc có kích thước lớn, kho lưu trữ này chỉ lưu trữ mã nguồn do nhóm phát triển thực hiện:

```text
MESH_DATN/
│
├── mbf_fac_Client_v1/          # Source code phía Client
│   └── sourcesFiles/src/       # Thư mục chứa mã nguồn C/C++ (main.c, v.v.)
│
├── .gitignore                  # Cấu hình bỏ qua các tệp rác và thư mục SDK nặng
└── README.md                   # Tài liệu hướng dẫn dự án

```

## ⚙️ Hướng dẫn cài đặt và chạy dự án

- copy nội dung vào file main.c của dự án
